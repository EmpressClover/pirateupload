# Admin access
ADMIN_PASSWORD=change-me
ADMIN_JWT_SECRET=change-me-to-a-long-random-string

# Bunny.net Storage (set these in Vercel)
BUNNY_STORAGE_ZONE=
BUNNY_ACCESS_KEY=
BUNNY_STORAGE_HOST=storage.bunnycdn.com
BUNNY_CDN_BASE_URL=

# Optional: Anonymous API keys (comma-separated). If set, /api/upload requires one.
# These keys are safe to expose in client apps; they only identify callers.
PUBLIC_UPLOAD_KEYS=
